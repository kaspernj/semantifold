Add Rust source and native Cargo-project generation for the Tasks001–004 scalar subset. The existing private parser runtime now includes qualified official Rust grammar0.23.1; source moves and temporary borrows are validated before exact clone/print/overflow scaffold collapse. Generated strings preserve value copies, Unicode/NUL bytes and native left-to-right/short-circuit evaluation without new semantic nodes.

Emit deterministic `Cargo.toml`, Cargo-qualified format-4 `Cargo.lock` and mapped `src/main.rs`, with complete pre-emission IR/name/size/depth checks and exact edition2021 Rust/Cargo1.98.1 build profiles. Dynamic integer overflow has the same status70 diagnostic in debug and release. Add checksum-pinned build-time tools, offline native/ownership/adversarial/provenance/original-five acceptance and cold root-package install/ci/types/runtime proof. Final coordinator review, exact-head full/native CI and merge remain delivery gates; this change does not publish a release.
