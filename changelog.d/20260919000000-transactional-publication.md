## Added

- Add `GeneratedArtifactPublisher` for filesystem-safe immutable project generations beneath a canonical project root, explicit target roles, validator/compiler by-products, complete hard-link-safe inventory, deterministic hashed manifests, one atomic active-generation pointer, pointer-once reader snapshots with truthful cleanup state, and crash-durable journal-bounded recovery.
