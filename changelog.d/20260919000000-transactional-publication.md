## Added

- Add `GeneratedArtifactPublisher` for filesystem-safe immutable project generations, validator/compiler by-products, deterministic hashed manifests, one atomic active-generation pointer, pointer-once reader snapshots, and journal-bounded interruption recovery.
