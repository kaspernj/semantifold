Added a target-only browser WebAssembly backend for the Tasks 001–004 semantic IR. It directly emits deterministic `program.wasm`, byte provenance and a Source Map v3 sidecar, a strict `semantifold.browser.v1` JavaScript loader, and an HTML harness whose CSP permits only same-origin modules plus narrow Wasm compilation. The runtime uses fixed memory, checked signed-i64 arithmetic, canonical Booleans, bounded UTF-8 scratch allocation, and call-depth/trap protections.

Added canonical WABT 1.0.36 and Chrome/Chromium 152.0.7977.82 discovery, pinned Docker/TensorBuzz delivery, and unavailable-fatal real validator, Node, and loopback headless-browser acceptance. Version discovery now prefers normalized non-empty stdout over incidental launcher stderr while retaining the exact pinned version policy.

Corrected byte provenance to use parser-owned semantic token ranges with synthetic binary framing, bounded malformed IR before recursive validation, and applied known-i64 checks to unused and unreachable code. The loader now starts native streaming before parallel ABI inspection while locking host imports, preserves leading U+FEFF string data, accepts valid `$` identifiers, retains timeout stages through body and instantiation aborts, and reports valid unsupported ABI value types as instantiate/import failures.
