- Wait for the first Android UI artifact through a bounded readiness protocol: while a fresh boot is still settling package-manager work, `uiautomator dump` can report a null root node and leave no remote file behind, so the dump and pull are retried as one unit up to 30 one-second attempts until a non-empty artifact lands locally; persistent failure still fails loudly with the existing artifact capture and the exact `UI artifact capture failed` reason, reusing the bounded readiness cadence the Flutter UI assertion already uses.
