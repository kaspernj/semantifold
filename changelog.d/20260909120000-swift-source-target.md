Add strict Swift source parsing and deterministic single-file `program.swift` generation for the Tasks001–004 scalar subset. The qualified MIT grammar packaging fork is pinned by immutable HTTPS commit archive; the adapter rejects recovery and unmodeled Swift while preserving UTF-16 provenance. Generated Swift retains explicit `Int64`, `Bool`, and `String` declarations, rich/V3 mappings, checked native arithmetic, and exact Unicode-scalar equality through fully validated target-only helpers.

Register Swift as a round-trip text language with exact Swift 6.3.3 Linux x86-64 discovery. Reject ordinary String `==`/`!=` and use only fully validated generated helpers for exact scalar equality; generated mutable locals use synthetic warning-clean `inout` scaffolding. TensorBuzz provisions the same signed Swift compiler through `SEMANTIFOLD_SWIFTC=/opt/swift-6.3.3-RELEASE-ubuntu24.04/usr/bin/swiftc`. The independent review's bounded correction is complete; exact-head TensorBuzz CI, merge, and post-merge verification remain delivery gates.
