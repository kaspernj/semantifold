Add invariant unbounded type parameters and closed generic declarations/applications for PHP, Ruby, JavaScript/JSDoc, TypeScript, and Java. Calls infer complete first-order substitutions from arguments, including whole optional bindings, present optional values, and evidence-free arguments validated after order-independent inference. Generic record construction, parser-known call results, and member contexts substitute fields recursively; Java rejects raw construction, non-generic record arguments are forbidden, and PHP generic getters resolve through exact direct or presence-proven optional receivers while property spelling remains rejected. Dynamic targets preserve recursive optional type variables and applied records in exact reparsable documentation. PHP omits invalid native `Optional<T>` carriers while requiring nullable native carriers that exactly match fixed optional scalar/record/collection documentation, and unsupported targets reject generic IR transactionally.
