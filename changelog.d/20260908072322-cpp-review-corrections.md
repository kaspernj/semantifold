Correct Task019's Go discovery assertion to include CPP while retaining Go's registry position and role contract. Reject CPP declarations named `WEOF` or `wint_t` before artifact generation because the selected `<string>` header environment exposes those names; preserve C's separate header contract and ordinary CPP identifiers. Reject raw CR/LF inside ordinary CPP string-content nodes with located diagnostics, while preserving escaped CR/LF, Unicode/NUL bytes and supported source line endings. Real Clang regressions reproduce both header collisions and the raw-CR parser/compiler mismatch.
