Fixed the Kotlin grammar trust boundary to use an integrity-locked full-SHA HTTPS archive without expanding root `acceptDependencies`, made Kotlin/JVM bootstrap validation accept only the qualified Ubuntu 24.04 or 26.04 JRE packaging suffix while retaining exact Kotlin and JDK versions, and made both canonical compiler bootstraps converge a stale dedicated install destination before moving the verified extraction into place.
