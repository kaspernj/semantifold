Add versioned canonical standard-library contract modules, the built-in `semantifold.task034.resource-probe@1.0.0` with five closed operations, a target host provider registry with protected native bindings, transitive capability negotiation, and transactional program-mode provider linking with tree-shaken, byte-stable provider artifacts for PHP, Ruby, JavaScript/JSDoc, and TypeScript. Java resolves the same negotiated provider record but hosts the shared support in the first operation-using module's generated class as public static nested classes with protected static shims, imported by non-owner modules, and reports that owner module file as the provider carrier. Compatibility facades and public host APIs remain excluded.
