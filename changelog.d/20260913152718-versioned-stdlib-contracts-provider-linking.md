Add versioned canonical standard-library contract modules, the built-in `semantifold.task034.resource-probe@1.0.0` with five closed operations, a target host provider registry with protected native bindings, transitive capability negotiation, and transactional program-mode provider linking with tree-shaken, byte-stable provider artifacts for PHP, Ruby, JavaScript/JSDoc, TypeScript, and Java. Compatibility facades and public host APIs remain excluded.
