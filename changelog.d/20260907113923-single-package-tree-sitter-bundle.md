Corrected Semantifold's package layout so the one root `semantifold@0.2.0` tarball explicitly bundles both root `tree-sitter@0.25.1` and a private internal legacy subtree containing exact `tree-sitter@0.21.1` and `tree-sitter-c@0.23.2`. Removed the standalone scoped adapter identity and separate-publication prerequisite while retaining its frozen parser-neutral serialization implementation as private code.
