Task 029 adds strict parser-native Dart source and deterministic dependency-free Dart VM/native package generation through Tasks 001–005. Exact public `tree-sitter-dart-orchard@0.7.0` and checksum-pinned Dart SDK 3.13.3 are qualified; generated `pubspec.yaml`, `pubspec.lock`, and mapped `bin/program.dart` round-trip and pass isolated offline format, fatal analysis, VM, native compilation, and native execution with checked safe integers and formatter-canonical over-width signatures. Exact wildcard `_` bindings and official formatter-control comments are rejected. Flutter, browser Dart, packages/imports, dynamic/nullability, collections/classes, async/isolates, and exceptions remain excluded.
