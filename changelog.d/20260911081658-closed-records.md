Add nominal closed immutable records, exact construction, and resolved member reads for PHP, Ruby, JavaScript/JSDoc, TypeScript, and Java, with optional-record `length`/`size` dispatch, Java `equals()` field dispatch, constructor-name no-shadowing, collision-safe JavaScript/Ruby freezing, deterministic target built-in collision checks, strict external-IR record shape validation, preserved empty-record frontend compatibility, PHP 8.2+ runtime qualification, and transactional rejection for every other registered target.
