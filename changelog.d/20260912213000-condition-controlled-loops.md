Add strict-Boolean condition-controlled loops for PHP, Ruby, JavaScript/JSDoc, TypeScript, and Java. Conditions execute before every iteration, `continue` returns to the next condition evaluation, and `break` exits the nearest loop. All supported loop kinds now carry deterministic identities and controls carry resolved targets; malformed, non-nearest, unresolved, cross-function, and unsupported-target IR fails transactionally.
