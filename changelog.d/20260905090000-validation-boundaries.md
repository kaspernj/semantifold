# Fixed

- Retain exactly the artifact content value validated by generated artifact-set construction.
- Reject incorrectly typed byte-map schema versions and subprocess timeouts beyond Node's supported timer range.
