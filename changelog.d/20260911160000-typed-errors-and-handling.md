Add exact nominal typed unchecked errors, raises, one typed handler, message reads, propagation, multi-file identity, and original-five generation/runtime support. Catch entry conservatively invalidates facts for protected-body assignments, JavaScript-family imported error aliases respect lexical shadowing, and backend preflight rejects PHP special catch bindings while preserving deterministic malformed-declaration diagnostics. Other registered targets now report the capability as unsupported explicitly.
