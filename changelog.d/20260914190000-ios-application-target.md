# Add deterministic iOS application generation

- Register target-only `ios` application generation for Task 010 programs and normalized single modules within the Task 022 Tasks 001–004 Swift subset.
- Emit deterministic captured semantic Swift, an unsigned SwiftUI/Xcode project model, closed configuration, exact hashed assets, tests, causal renderer-owned configuration spans, and a versioned ownership/provenance manifest with stable semantic-program identities and generated linkage.
- Restrict artifact/resource paths to a completely comparable portable ASCII alphabet while retaining Unicode semantic source filenames, text, values, and generated output.
- Prove Ruby-to-generated-Swift output on Linux and deterministic generation through credential-free packed install and clean `npm ci` consumers, including proof that no materializer implementation or declaration is shipped.
- Materialization remains deferred because the full atomic no-replace and descriptor-relative no-follow contract requires a separately authorized native boundary; no best-effort writer is exposed.
- Apple acceptance remains deferred: no Xcode, Apple SDK, XCTest, XCUIAutomation, signing, or iOS Simulator proof is claimed, and Task 026 remains in progress.
