# Add deterministic iOS application generation

- Register target-only `ios` application generation for Task 010 programs and normalized single modules within the Task 022 Tasks 001–004 Swift subset.
- Emit deterministic captured semantic Swift, an unsigned SwiftUI/Xcode project model, closed configuration, exact hashed assets, tests, and a versioned ownership/provenance manifest.
- Add explicit create-only transactional artifact-set materialization with portable-path and symlink preflight, exclusive private staging, atomic publication, and no overwrite/regeneration behavior.
- Prove Ruby-to-generated-Swift output on Linux and generation/materialization through credential-free packed install and clean `npm ci` consumers.
- Apple acceptance remains deferred: no Xcode, Apple SDK, XCTest, XCUIAutomation, signing, or iOS Simulator proof is claimed, and Task 026 remains in progress.
