Reject stale installed private-runtime files before tests, lint, or root packaging, with normal `npm ci` as the recovery after source edits. Verify all shipped internal payload bytes against source and the real root archive. Isolate packed-consumer npm user/global configuration, remove inherited npm configuration and tokens case-insensitively, and verify the explicit public registry before a fresh-cache installation.

Support ordinary consumer npm defaults with an exact `acceptDependencies` declaration for the private payload already physically bundled by the root, retaining the local `file:` installation source. Assert default `install-links=false`, clean full dependency graphs, and repeat parsing/type checks after both fresh installation and subsequent `npm ci`. No consumer configuration or internal registry publication is required.
