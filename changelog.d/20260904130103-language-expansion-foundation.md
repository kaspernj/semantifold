# Added

- Add one immutable role/capability registry, deterministic single-module artifact sets, byte-range provenance, and fail-loud staged toolchain acceptance without expanding the supported language or semantic subset.
