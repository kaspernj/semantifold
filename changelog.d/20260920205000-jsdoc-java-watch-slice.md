Task 044 ships a copy-ready JavaScript/JSDoc-to-Java project and proves packed `semantifold watch --check` failure recovery, coherent real-`javac` publication, and explicit acceptance-only Java execution.

NDJSON watch reporting now emits one nonterminal `cycle-dirty` acknowledgement when an active cycle first admits a newer filesystem hint.
